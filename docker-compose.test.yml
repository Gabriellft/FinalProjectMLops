version: '3.8'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm help

  backend:
    build:
      context: ./BACKEND
      dockerfile: Dockerfile
    volumes:
      - ./BACKEND:/app
      - /app/node_modules
    command: npm test
